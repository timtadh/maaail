A Command Line Email Sender
===========================

A Tim|Steve Production

A nice way to send email from the command line. Write in anything `pandoc`
supports. Send multi-part email with usable plain text and html. Attachments are
supported.

